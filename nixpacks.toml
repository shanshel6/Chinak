[phases.setup]
# Chromium + GTK/X11 libs removed: the API no longer launches puppeteer
# (on-demand image scraping moved out of the server). Scraping runs in the
# local pipeline, not on Railway.
aptPkgs = ["ca-certificates"]
